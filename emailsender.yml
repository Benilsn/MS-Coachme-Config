server:
  port: 8081

eureka:
  instance:
    hostname: localhost
    port: 9000
  client:
    registerWithEureka: true
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${eureka.instance.port}/eureka/
    server:
      wait-time-in-ms-when-sync-empty: 3000

spring:
  rabbitmq:
    addresses: amqps://uvigbyif:R12FKow9Qqoag_FO6PWhx3ZV3T-ZiF8M@jackal.rmq.cloudamqp.com/uvigbyif
    queue: coachme_queue
    routingKey: coachme_routingKey
    exchange: coachme_exchange